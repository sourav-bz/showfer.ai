/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.0 public/models/8th_Character.gltf -o src/app/_ui/Characters/Showfer8.tsx -r public 
*/
// @ts-nocheck

import * as THREE from "three";
import React, { useEffect } from "react";
import { useGLTF } from "@react-three/drei";
import { GLTF } from "three-stdlib";

type GLTFResult = GLTF & {
  nodes: {
    ["8th_Character"]: THREE.Mesh;
  };
  materials: {
    Image: THREE.MeshStandardMaterial;
  };
  animations: GLTFAction[];
};

export default function Aman(props) {
  const { nodes, materials } = useGLTF(
    "/models/8th_Character.gltf"
  ) as GLTFResult;

  useEffect(() => {
    if (!props.storyState) return;

    let animationFrameId;
    let talkValue = 0;

    const animateTalk = () => {
      if (props.storyState === "assistant") {
        // Toggle between 0 and 1
        talkValue = talkValue === 0 ? 1 : 0;

        nodes["8th_Character"].morphTargetInfluences[
          nodes["8th_Character"].morphTargetDictionary["Happy_Talk"]
        ] = talkValue;

        // Request next frame after a delay to control the speed of the animation
        animationFrameId = setTimeout(() => {
          requestAnimationFrame(animateTalk);
        }, 250); // Adjust this value to change the speed of the mouth movement
      }
    };

    animateTalk();

    return () => {
      if (animationFrameId) {
        clearTimeout(animationFrameId);
      }
    };
  }, [props.storyState, nodes["8th_Character"]]);

  useEffect(() => {
    console.log(
      "shape keys dictionary 2b",
      nodes["8th_Character"].morphTargetDictionary
    );
    console.log(
      "shape keys influences 2b",
      nodes["8th_Character"].morphTargetInfluences
    );

    nodes["8th_Character"].morphTargetInfluences[
      nodes["8th_Character"].morphTargetDictionary["Default_Pose"]
    ] = 1;
  }, []);

  return (
    <group {...props} dispose={null}>
      <mesh
        name="8th_Character"
        geometry={nodes["8th_Character"].geometry}
        material={materials.Image}
        morphTargetDictionary={nodes["8th_Character"].morphTargetDictionary}
        morphTargetInfluences={nodes["8th_Character"].morphTargetInfluences}
      />
    </group>
  );
}

useGLTF.preload("/models/8th_Character.gltf");
